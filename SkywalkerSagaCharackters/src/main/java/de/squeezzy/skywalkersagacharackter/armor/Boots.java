package de.squeezzy.skywalkersagacharackter.armor;

import org.bukkit.Color;
import org.bukkit.Material;
import org.bukkit.inventory.ItemStack;
import org.bukkit.inventory.meta.LeatherArmorMeta;

public class Boots {

    public static ItemStack getAnakin() {
        // Got this base64 string from minecraft-heads.com
        ItemStack boots = new ItemStack(Material.LEATHER_BOOTS);
        LeatherArmorMeta meta = (LeatherArmorMeta) boots.getItemMeta();
        assert meta != null;
        meta.setColor(Color.fromRGB(23, 3, 0));
        boots.setItemMeta(meta);
        return boots;
    }

    public static ItemStack getAhsoka() {
        // Got this base64 string from minecraft-heads.com
        ItemStack boots = new ItemStack(Material.LEATHER_BOOTS);
        LeatherArmorMeta meta = (LeatherArmorMeta) boots.getItemMeta();
        assert meta != null;
        meta.setColor(Color.MAROON);
        boots.setItemMeta(meta);
        return boots;
    }

    public static ItemStack getLuke() {
        ItemStack boots = new ItemStack(Material.LEATHER_BOOTS);
        LeatherArmorMeta meta = (LeatherArmorMeta) boots.getItemMeta();
        assert meta != null;
        meta.setColor(Color.GRAY);
        boots.setItemMeta(meta);
        return boots;
    }

    public static ItemStack getYoda() {
        ItemStack boots = new ItemStack(Material.LEATHER_BOOTS);
        LeatherArmorMeta meta = (LeatherArmorMeta) boots.getItemMeta();
        assert meta != null;
        meta.setColor(Color.SILVER);
        boots.setItemMeta(meta);
        return boots;
    }

    public static ItemStack getRey() {
        // Got this base64 string from minecraft-heads.com
        ItemStack chestplate = new ItemStack(Material.LEATHER_BOOTS);
        LeatherArmorMeta meta = (LeatherArmorMeta) chestplate.getItemMeta();
        assert meta != null;
        meta.setColor(Color.SILVER);
        chestplate.setItemMeta(meta);
        return chestplate;
    }

    //Held
    public static ItemStack getLeia() {
        // Got this base64 string from minecraft-heads.com
        ItemStack chestplate = new ItemStack(Material.LEATHER_BOOTS);
        LeatherArmorMeta meta = (LeatherArmorMeta) chestplate.getItemMeta();
        assert meta != null;
        meta.setColor(Color.BLACK);
        chestplate.setItemMeta(meta);
        return chestplate;
    }

    public static ItemStack getJarJar() {
        return null;
    }

    public static ItemStack getEwok() {
        ItemStack chestplate = new ItemStack(Material.LEATHER_BOOTS);
        LeatherArmorMeta meta = (LeatherArmorMeta) chestplate.getItemMeta();
        assert meta != null;
        meta.setColor(Color.fromRGB(139, 69, 19));
        chestplate.setItemMeta(meta);
        return chestplate;
    }

    public static ItemStack getRebelPilot() {
        ItemStack chestplate = new ItemStack(Material.LEATHER_BOOTS);
        LeatherArmorMeta meta = (LeatherArmorMeta) chestplate.getItemMeta();
        assert meta != null;
        meta.setColor(Color.WHITE);
        chestplate.setItemMeta(meta);
        return chestplate;
    }

    public static ItemStack getRebelHoth() {
        ItemStack chestplate = new ItemStack(Material.LEATHER_BOOTS);
        LeatherArmorMeta meta = (LeatherArmorMeta) chestplate.getItemMeta();
        assert meta != null;
        meta.setColor(Color.WHITE);
        chestplate.setItemMeta(meta);
        return chestplate;
    }

    public static ItemStack getReySchrott() {
        ItemStack boots = new ItemStack(Material.LEATHER_BOOTS);
        LeatherArmorMeta meta = (LeatherArmorMeta) boots.getItemMeta();
        assert meta != null;
        meta.setColor(Color.fromRGB(143, 63, 10));
        boots.setItemMeta(meta);
        return boots;
    }

    public static ItemStack getJawa() {
        ItemStack chestplate = new ItemStack(Material.LEATHER_BOOTS);
        LeatherArmorMeta meta = (LeatherArmorMeta) chestplate.getItemMeta();
        assert meta != null;
        meta.setColor(Color.BLACK);
        chestplate.setItemMeta(meta);
        return chestplate;
    }
    public static ItemStack getTusken() {
        ItemStack chestplate = new ItemStack(Material.LEATHER_BOOTS);
        LeatherArmorMeta meta = (LeatherArmorMeta) chestplate.getItemMeta();
        assert meta != null;
        meta.setColor(Color.fromRGB(143, 63, 10));
        chestplate.setItemMeta(meta);
        return chestplate;
    }

    public static ItemStack getWicket() {
        // Got this base64 string from minecraft-heads.com
        ItemStack chestplate = new ItemStack(Material.LEATHER_BOOTS);
        LeatherArmorMeta meta = (LeatherArmorMeta) chestplate.getItemMeta();
        assert meta != null;
        meta.setColor(Color.fromRGB(139,69,19));
        chestplate.setItemMeta(meta);
        return chestplate;
    }

    public static ItemStack getChiefChirpa() {
        // Got this base64 string from minecraft-heads.com
        ItemStack chestplate = new ItemStack(Material.LEATHER_BOOTS);
        LeatherArmorMeta meta = (LeatherArmorMeta) chestplate.getItemMeta();
        assert meta != null;
        meta.setColor(Color.fromRGB(139,69,19));
        chestplate.setItemMeta(meta);
        return chestplate;
    }

    //Schurke
    public static ItemStack getHanSolo() {
        // Got this base64 string from minecraft-heads.com
        ItemStack chestplate = new ItemStack(Material.LEATHER_BOOTS);
        LeatherArmorMeta meta = (LeatherArmorMeta) chestplate.getItemMeta();
        assert meta != null;
        meta.setColor(Color.BLACK);
        chestplate.setItemMeta(meta);
        return chestplate;
    }

    public static ItemStack getChewbacca() {
        // Got this base64 string from minecraft-heads.com
        ItemStack chestplate = new ItemStack(Material.LEATHER_BOOTS);
        LeatherArmorMeta meta = (LeatherArmorMeta) chestplate.getItemMeta();
        assert meta != null;
        meta.setColor(Color.fromRGB(139,69,19));
        chestplate.setItemMeta(meta);
        return chestplate;
    }

    public static ItemStack getWookie() {
        // Got this base64 string from minecraft-heads.com
        ItemStack chestplate = new ItemStack(Material.LEATHER_BOOTS);
        LeatherArmorMeta meta = (LeatherArmorMeta) chestplate.getItemMeta();
        assert meta != null;
        meta.setColor(Color.fromRGB(139,69,19));
        chestplate.setItemMeta(meta);
        return chestplate;
    }

    //Kopfgeldjäger
    public static ItemStack getBobaFett() {
        // Got this base64 string from minecraft-heads.com
        ItemStack chestplate = new ItemStack(Material.LEATHER_BOOTS);
        LeatherArmorMeta meta = (LeatherArmorMeta) chestplate.getItemMeta();
        assert meta != null;
        meta.setColor(Color.fromRGB(35, 99, 43));
        chestplate.setItemMeta(meta);
        return chestplate;
    }

    public static ItemStack getHondo() {
        // Got this base64 string from minecraft-heads.com
        ItemStack chestplate = new ItemStack(Material.LEATHER_BOOTS);
        LeatherArmorMeta meta = (LeatherArmorMeta) chestplate.getItemMeta();
        assert meta != null;
        meta.setColor(Color.BLACK);
        chestplate.setItemMeta(meta);
        return chestplate;
    }

    public static ItemStack getJabba() {
        // Got this base64 string from minecraft-heads.com
        ItemStack chestplate = new ItemStack(Material.LEATHER_BOOTS);
        LeatherArmorMeta meta = (LeatherArmorMeta) chestplate.getItemMeta();
        assert meta != null;
        meta.setColor(Color.fromRGB(188, 204, 177));
        chestplate.setItemMeta(meta);
        return chestplate;
    }

    public static ItemStack getTarkin() {
        // Got this base64 string from minecraft-heads.com
        ItemStack chestplate = new ItemStack(Material.LEATHER_BOOTS);
        LeatherArmorMeta meta = (LeatherArmorMeta) chestplate.getItemMeta();
        assert meta != null;
        meta.setColor(Color.BLACK);
        chestplate.setItemMeta(meta);
        return chestplate;
    }

    public static ItemStack getVader() {
        // Got this base64 string from minecraft-heads.com
        ItemStack chestplate = new ItemStack(Material.LEATHER_BOOTS);
        LeatherArmorMeta meta = (LeatherArmorMeta) chestplate.getItemMeta();
        assert meta != null;
        meta.setColor(Color.BLACK);
        chestplate.setItemMeta(meta);
        return chestplate;
    }

    public static ItemStack getR2D2() {
        // Got this base64 string from minecraft-heads.com
        ItemStack chestplate = new ItemStack(Material.LEATHER_BOOTS);
        LeatherArmorMeta meta = (LeatherArmorMeta) chestplate.getItemMeta();
        assert meta != null;
        meta.setColor(Color.GRAY);
        chestplate.setItemMeta(meta);
        return chestplate;
    }

    public static ItemStack getC3PO() {
        // Got this base64 string from minecraft-heads.com
        ItemStack chestplate = new ItemStack(Material.LEATHER_BOOTS);
        LeatherArmorMeta meta = (LeatherArmorMeta) chestplate.getItemMeta();
        assert meta != null;
        meta.setColor(Color.fromRGB(201, 163, 73));
        chestplate.setItemMeta(meta);
        return chestplate;
    }
}
